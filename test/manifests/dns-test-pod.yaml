apiVersion: v1
kind: Pod
metadata:
  name: dns-test-pod
  namespace: noc-poc
  labels:
    app: dns-test
spec:
  containers:
    - name: dnsutils
      image: registry.k8s.io/e2e-test-images/agnhost:2.39
      command: ["sleep", "infinity"]
  restartPolicy: Never
