apiVersion: v1
kind: Service
metadata:
  name: unbound
  labels:
    app: unbound
    app.kubernetes.io/part-of: noc-poc
spec:
  type: LoadBalancer
  selector:
    app: unbound
  ports:
    - name: dns-udp
      port: 53
      targetPort: 53
      protocol: UDP
    - name: dns-tcp
      port: 53
      targetPort: 53
      protocol: TCP
