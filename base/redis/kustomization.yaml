apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml

configMapGenerator:
  - name: redis-config
    files:
      - configs/redis.conf
    options:
      disableNameSuffixHash: true
